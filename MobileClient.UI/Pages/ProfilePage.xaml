<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:View="clr-namespace:MobileClient.UI.Pages.Views"
             x:Class="MobileClient.UI.Pages.ProfilePage"
             Title="ProfilePage">

    <ContentPage.Resources>
        <ResourceDictionary>
            <View:CarouselDataTemplateSelector x:Key="CarouselDataTemplateSelector"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid Padding="20"
              ColumnDefinitions="*, *"
              RowDefinitions="Auto, *">
            <CarouselView Grid.Column="0"
                     Grid.ColumnSpan="2"
                     Grid.Row="1"
                     ItemTemplate="{StaticResource CarouselDataTemplateSelector}"
                     IsSwipeEnabled="False"
                     IsScrollAnimated="False"
                     Position="{Binding PositionSelected}">
                <CarouselView.ItemsSource>
                    <x:Array Type="{x:Type x:String}">
                        <x:String>1</x:String>
                        <x:String>2</x:String>
                    </x:Array>
                </CarouselView.ItemsSource>
            </CarouselView>
        </Grid>
    </ContentPage.Content>
</ContentPage>